{% extends '::header_base.html.twig' %}


{% block navbar_content %}
    {# {% if is_granted('IS_AUTHENTICATED_FULLY') %} #}
        <ul class="nav navbar-nav">
            <li><a href="{{ path('homepage') }}">RedHat Search<span class="sr-only">(current)</span></a></li>
            <li><a href="{{ path('gui_triage_list_page') }}">RHEL Triage</a></li>
            <li class="active"><a href="{{ path('gui_issue_list_page') }}">Issues</a></li>
        </ul>
    {# {% endif %} #}
{% endblock %}
